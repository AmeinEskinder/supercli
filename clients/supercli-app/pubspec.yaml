name: supercli_app
description: supercli desktop app on gpuidart (macOS/Linux first).
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  gpuidart:
    path: ../gpuidart
  http: ^1.2.0

dev_dependencies:
  test: ^1.26.0
  lints: ^6.0.0
  mocktail: ^1.0.0
